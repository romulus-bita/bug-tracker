pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "Hello Jenkins from my 2nd pipeline"'
                sh 'date'
            }
        }
    }
}
